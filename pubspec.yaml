name: restapi_flutter
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.3
  equatable: ^1.2.5
  json_serializable: ^3.5.1
  dio: ^3.0.10
  bloc: ^6.0.3
  flutter_bloc: ^6.0.6

dev_dependencies:
  flutter_driver:
    sdk: flutter
  flutter_test:
    sdk: flutter
  test: any
  build_runner: any

flutter:
  uses-material-design: true

  assets:
    - assets/
